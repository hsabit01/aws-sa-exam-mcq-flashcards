| Topic | Choose this when… | Do NOT choose when… | Key exam trigger words | Related services |
|---|---|---|---|---|
| IAM users, groups, policies | Need long-term human or app identities and permissions.<br>- Users = identities<br>- Groups = permission management<br>- Policies = allow or deny actions | Do not use users for AWS services running on EC2, Lambda, ECS.<br>Use IAM roles instead. | users, groups, permissions, least privilege, identity-based policy | IAM roles, IAM policies, IAM Identity Center, AWS Organizations |
| IAM roles for AWS services | Need temporary credentials for AWS services to access other AWS services. | Do not store access keys on EC2, Lambda, ECS, or applications. | temporary credentials, assume role, EC2 access S3, Lambda access DynamoDB | IAM users, STS, EC2 instance profile, Lambda execution role |
| IAM policy evaluation logic | Need to know if a request is allowed or denied.<br>Explicit deny always wins. | Do not assume allow from one policy overrides an explicit deny. | explicit deny, default deny, allow, policy evaluation, permissions boundary, SCP | IAM policies, SCP, resource policies, permission boundaries |
| IAM conditions | Need permission rules based on context like IP, MFA, tags, time, encryption, or VPC endpoint. | Do not use conditions when simple action/resource permission is enough. | MFA required, source IP, request tag, aws:SourceVpce, encryption required | IAM policies, S3 bucket policies, KMS key policies, SCP |
| IAM permission boundaries | Need maximum permissions a user or role can ever receive. | Do not use as a direct grant of permissions.<br>It only limits permissions. | maximum permissions, delegate admin, limit developer-created roles | IAM policies, SCP, IAM roles, IAM users |
| IAM resource-based policies vs IAM roles | Use resource policy when the resource grants access directly.<br>Use role when an identity needs temporary permissions. | Do not confuse trust policy with permission policy.<br>Roles are assumed; resource policies are attached to resources. | cross-account S3 access, bucket policy, assume role, resource grants access | S3 bucket policy, Lambda resource policy, KMS key policy, IAM roles |
| IAM Identity Center | Need centralized workforce access to multiple AWS accounts and apps with SSO. | Do not create many IAM users for employees across accounts. | SSO, workforce identity, multiple accounts, permission sets | AWS Organizations, IAM, external IdP, Active Directory |
| AWS Organizations | Need centralized management of many AWS accounts. | Do not use for application-level permissions inside one account only. | multiple accounts, consolidated billing, account hierarchy, central governance | Organizational Units, SCP, Control Tower, IAM Identity Center |
| Organizational Units | Need to group AWS accounts for policy and governance management. | Do not use OUs as billing resources or IAM groups. | group accounts, dev OU, prod OU, policy inheritance | AWS Organizations, SCP, Control Tower |
| Service Control Policies / organization policies | Need maximum permission guardrails across AWS accounts in an organization. | Do not use SCPs to grant permissions.<br>SCPs only limit what accounts can do. | deny regions, prevent disabling CloudTrail, organization-wide guardrail | AWS Organizations, OUs, IAM policies, Control Tower guardrails |
| AWS Control Tower | Need automated multi-account landing zone with best-practice governance. | Do not use when you only need one account or simple billing consolidation. | landing zone, account factory, governance, multi-account setup | AWS Organizations, SCP, Guardrails, IAM Identity Center |
| AWS Control Tower guardrails | Need preventive or detective governance controls across accounts. | Do not use for app runtime security rules like WAF web filtering. | guardrails, detective control, preventive control, compliance baseline | SCP, AWS Config, AWS Organizations, Security Hub |
| AWS KMS | Need managed encryption keys and key access control for AWS services. | Do not use for storing secrets directly.<br>Use Secrets Manager or Parameter Store. | encryption keys, customer managed key, envelope encryption, key rotation | Secrets Manager, SSM Parameter Store, CloudHSM, S3 SSE-KMS |
| KMS key types | Use AWS owned, AWS managed, or customer managed keys depending on control needed. | Do not choose AWS managed keys when exam requires key policy control or manual rotation. | AWS owned key, AWS managed key, customer managed key, CMK, key rotation | AWS KMS, CloudHSM, S3 SSE-KMS |
| KMS multi-Region keys | Need same logical KMS key material replicated across Regions for client-side or app encryption. | Do not use only for S3 CRR unless normal SSE-KMS replication is enough. | multi-Region encryption, same key ID, disaster recovery, global app | AWS KMS, S3 replication, DynamoDB global tables |
| S3 encryption: SSE-S3, SSE-KMS, SSE-C | Use SSE-S3 for simple AWS-managed encryption.<br>Use SSE-KMS for audit and key control.<br>Use SSE-C when customer supplies keys. | Do not choose SSE-C if AWS must manage keys.<br>Do not choose SSE-S3 when key-level audit is required. | server-side encryption, KMS audit, customer-provided key, encryption at rest | S3, KMS, CloudTrail, IAM policies |
| S3 client-side encryption | Need data encrypted before it reaches S3. | Do not use when server-side encryption meets the requirement. | encrypt before upload, client controls encryption, data encrypted in transit and at rest | S3 SSE, AWS Encryption SDK, KMS |
| S3 in-transit encryption | Need data protected while moving to or from S3 using HTTPS/TLS. | Do not confuse with encryption at rest. | HTTPS only, TLS, secure transport, aws:SecureTransport | S3 bucket policy, CloudFront, ACM |
| SSM Parameter Store | Need configuration values or basic secrets, often lower cost. | Do not choose for automatic secret rotation with database credentials.<br>Use Secrets Manager. | config values, hierarchy, standard parameter, secure string | Secrets Manager, KMS, Systems Manager |
| AWS Secrets Manager | Need managed secrets with rotation, especially database credentials. | Do not use for simple non-secret config values.<br>Use Parameter Store. | secret rotation, database password, credentials, RDS rotation | SSM Parameter Store, KMS, RDS, Lambda |
| AWS Certificate Manager | Need provisioned and renewed public TLS certificates for AWS integrated services. | Do not use public ACM certs directly on EC2 without supported integration. | SSL certificate, TLS certificate, automatic renewal, HTTPS | ALB, CloudFront, API Gateway, Route 53 |
| ACM with ALB | Use ACM certificate on an Application Load Balancer for HTTPS websites or APIs. | Do not install ACM public cert directly inside EC2 manually. | HTTPS listener, ALB certificate, TLS termination | ACM, ALB, Route 53, EC2 |
| ACM with API Gateway | Use ACM certificate for custom domain HTTPS on API Gateway. | Do not use if no custom domain or TLS requirement exists. | custom domain, HTTPS API, regional certificate, edge-optimized API | API Gateway, ACM, CloudFront, Route 53 |
| AWS WAF | Need layer 7 web protection against HTTP attacks. | Do not use for DDoS infrastructure protection alone.<br>Use Shield. | SQL injection, XSS, HTTP rules, web ACL, block IPs | Shield, Firewall Manager, CloudFront, ALB, API Gateway |
| AWS Shield | Need DDoS protection.<br>Standard is automatic; Advanced adds enhanced protection. | Do not use for SQL injection or XSS filtering.<br>Use WAF. | DDoS, SYN flood, volumetric attack, Shield Advanced | WAF, CloudFront, Route 53, Global Accelerator |
| AWS Firewall Manager | Need central management of WAF, Shield, security groups, or firewall rules across accounts. | Do not use for single-account manual firewall setup. | central security policy, multiple accounts, enforce WAF rules | AWS Organizations, WAF, Shield, Security Groups, Network Firewall |
| WAF vs Shield vs Firewall Manager | Choose WAF for web layer rules.<br>Choose Shield for DDoS.<br>Choose Firewall Manager for central rule management. | Do not choose WAF for all DDoS cases or Firewall Manager for direct traffic filtering. | web attacks vs DDoS vs central management | WAF, Shield, Firewall Manager, CloudFront |
| Amazon GuardDuty | Need intelligent threat detection from logs and network activity. | Do not use for vulnerability scanning.<br>Use Inspector. | threat detection, suspicious API calls, crypto mining, compromised instance | CloudTrail, VPC Flow Logs, DNS logs, Security Hub |
| Amazon Inspector | Need automated vulnerability scanning for EC2, ECR, and Lambda. | Do not use for sensitive data discovery in S3.<br>Use Macie. | CVE, vulnerability, package scan, container image scan | GuardDuty, Security Hub, ECR, EC2, Lambda |
| Amazon Macie | Need discovery of sensitive data like PII in S3. | Do not use for malware or vulnerability scanning. | PII, sensitive data, S3 data discovery, personal information | S3, GuardDuty, Inspector, Security Hub |
| S3 bucket policies | Need resource-based access control directly on an S3 bucket. | Do not use when identity-level permissions are enough and no resource-level condition is needed. | bucket policy, cross-account S3 access, deny public, HTTPS only | IAM policies, S3 ACLs, S3 Block Public Access |
| S3 IAM access | Need identity-based access for users, groups, or roles to S3 actions. | Do not rely only on IAM if bucket policy has explicit deny. | allow user to read bucket, role access S3, identity policy | S3 bucket policy, IAM roles, IAM users |
| S3 Block Public Access | Need to prevent accidental public exposure of S3 buckets or objects. | Do not disable unless public access is intentionally required, like static website hosting. | block public access, prevent public bucket, account-level block | S3 bucket policy, ACLs, IAM Access Analyzer |
| S3 pre-signed URLs | Need temporary access to private S3 objects without changing bucket permissions. | Do not use for long-term public hosting.<br>Use CloudFront or public website hosting. | temporary download, limited time access, private object sharing | S3, CloudFront signed URLs, IAM |
| S3 access points | Need simplified access management for shared buckets across many apps, teams, or VPCs. | Do not use for small simple buckets with one access pattern. | access point, many applications, VPC-only S3 access | S3 bucket policy, IAM, VPC endpoints |
| S3 Object Lock | Need WORM protection for S3 objects to prevent deletion or overwrite. | Do not use after bucket creation unless versioning/Object Lock requirements were planned. | WORM, compliance mode, governance mode, retention, legal hold | S3 Versioning, Glacier Vault Lock, AWS Backup Vault Lock |
| Glacier Vault Lock | Need WORM compliance lock for S3 Glacier vault policies. | Do not choose for normal S3 objects.<br>Use S3 Object Lock. | vault lock, archive compliance, immutable archives | S3 Object Lock, S3 Glacier, AWS Backup Vault Lock |
| VPC | Need isolated virtual network for AWS resources. | Do not use as a compute service itself. | private network, CIDR block, subnets, route tables | Subnets, Route Tables, IGW, NAT Gateway, Security Groups |
| Subnets | Need to divide a VPC by Availability Zone and public/private routing. | Do not span a subnet across multiple AZs. | public subnet, private subnet, AZ, CIDR range | VPC, Route Tables, NAT Gateway, IGW |
| Internet Gateway | Need public internet access for resources in public subnets. | Do not use alone for private subnet outbound access.<br>Use NAT Gateway. | public subnet, internet access, route 0.0.0.0/0 to IGW | NAT Gateway, Route Table, Elastic IP |
| NAT Gateway | Need managed outbound internet access for private subnet resources. | Do not use for inbound internet access.<br>Do not ignore per-AZ cost and data charges. | private subnet outbound, patch updates, no inbound access, managed NAT | NAT Instance, Internet Gateway, Egress-only IGW |
| NAT Instance | Need customizable NAT or low-cost legacy NAT, managed by you. | Do not choose when high availability and managed scaling are required.<br>Use NAT Gateway. | custom NAT, legacy NAT, self-managed, lower cost | NAT Gateway, EC2, Security Groups |
| Security Groups | Need stateful instance-level firewall rules. | Do not use for explicit deny rules.<br>Use NACLs for subnet-level deny. | stateful firewall, allow rules only, instance firewall | NACLs, ENI, EC2, ALB |
| Network ACLs | Need stateless subnet-level firewall with allow and deny rules. | Do not use as primary app firewall when security groups are enough. | subnet firewall, stateless, explicit deny, numbered rules | Security Groups, Subnets, AWS Network Firewall |
| Stateless vs stateful firewalls | Stateful automatically allows return traffic.<br>Stateless requires inbound and outbound rules. | Do not forget ephemeral ports for NACL return traffic. | return traffic, ephemeral ports, security group vs NACL | Security Groups, NACLs, Network Firewall |
| VPC Peering | Need private connectivity between two VPCs. | Do not use for transitive routing or many VPC hub-and-spoke designs.<br>Use Transit Gateway. | connect two VPCs, private IP, no transitive routing | Transit Gateway, PrivateLink, VPC Endpoints |
| VPC Endpoints | Need private access from VPC to AWS services without internet or NAT. | Do not use NAT Gateway when private AWS service access is specifically required. | private AWS service access, no internet, gateway endpoint, interface endpoint | PrivateLink, S3 Gateway Endpoint, DynamoDB Endpoint, NAT Gateway |
| PrivateLink concept | Need private connectivity to a service using interface endpoints without exposing network routing. | Do not use when full VPC-to-VPC routing is needed.<br>Use peering or Transit Gateway. | private service access, interface endpoint, endpoint service, consumer/provider | VPC Endpoints, NLB, VPC Peering, Transit Gateway |
| VPC Flow Logs | Need network traffic metadata for VPC, subnet, or ENI. | Do not use for packet payload capture.<br>Use traffic mirroring. | accepted/rejected traffic, IP traffic logs, network troubleshooting | CloudWatch Logs, S3, Athena, GuardDuty |
| Site-to-Site VPN | Need encrypted connection from on-premises network to AWS over the internet. | Do not choose when dedicated private bandwidth is required.<br>Use Direct Connect. | IPsec VPN, on-prem to AWS, encrypted tunnel | Virtual Private Gateway, Transit Gateway, Direct Connect |
| Virtual Private Gateway | Need VPN or Direct Connect attachment to a single VPC. | Do not use for large hub-and-spoke multi-VPC routing.<br>Use Transit Gateway. | VGW, attach to VPC, VPN endpoint | Site-to-Site VPN, Direct Connect, Transit Gateway |
| Direct Connect | Need dedicated private network connection from on-premises to AWS. | Do not choose for quick encrypted setup over internet.<br>Use Site-to-Site VPN. | dedicated connection, private link, consistent bandwidth, hybrid network | Direct Connect Gateway, VPN, Transit Gateway |
| Direct Connect Gateway | Need Direct Connect connectivity to multiple VPCs across Regions. | Do not use as a replacement for Transit Gateway routing between VPCs. | DX to multiple VPCs, cross-Region private connectivity | Direct Connect, Transit Gateway, Virtual Private Gateway |
| Transit Gateway | Need hub-and-spoke routing among many VPCs and on-prem networks. | Do not use simple VPC peering for large transitive routing designs. | many VPCs, transitive routing, central hub, shared services VPC | VPC Peering, VPN, Direct Connect, Route Tables |
| Egress-only Internet Gateway | Need outbound-only IPv6 internet access from private subnets. | Do not use for IPv4.<br>Use NAT Gateway for IPv4 outbound. | IPv6 outbound only, no inbound IPv6 | NAT Gateway, Internet Gateway, IPv6 |
| AWS Network Firewall | Need managed network firewall with deep packet inspection and stateful rules in a VPC. | Do not use for layer 7 web app rules.<br>Use WAF. | managed firewall, intrusion prevention, domain filtering, VPC firewall | WAF, NACLs, Security Groups, Firewall Manager |
| VPC traffic mirroring | Need to copy network packets from ENIs for inspection or monitoring. | Do not use for basic metadata logging.<br>Use VPC Flow Logs. | packet capture, IDS, traffic analysis, mirror packets | VPC Flow Logs, Network Firewall, GuardDuty |
| Networking costs | Choose private connectivity and endpoints to reduce NAT/data transfer costs when appropriate. | Do not ignore cross-AZ, NAT Gateway, inter-Region, and internet egress charges. | data transfer cost, NAT charges, cross-AZ traffic, endpoint cost | NAT Gateway, VPC Endpoints, CloudFront, Direct Connect |
| Route 53 hosted zones | Need DNS zone management for a domain or private VPC DNS. | Do not use public hosted zone for private-only internal DNS. | public hosted zone, private hosted zone, DNS records | Route 53 records, VPC, Registrar |
| Route 53 record types | Need DNS records like A, AAAA, CNAME, MX, TXT, NS, or alias. | Do not use CNAME at zone apex.<br>Use alias where supported. | A record, CNAME, MX, TXT, alias, zone apex | Alias records, Hosted Zones, CloudFront, ALB |
| Route 53 TTL | Need to control DNS caching duration. | Do not set high TTL when fast failover or quick changes are required. | DNS cache, time to live, record caching | Route 53 records, Health Checks |
| Route 53 alias records | Need DNS record pointing to AWS resources with no extra DNS charge and zone apex support. | Do not use CNAME at zone apex. | alias to ALB, alias to CloudFront, root domain, zone apex | CNAME, A record, ALB, CloudFront, S3 website |
| Simple routing policy | Need one record answer without advanced routing logic. | Do not use when weighted, latency, failover, or geo routing is needed. | single endpoint, basic DNS routing | Weighted, Latency, Failover, Geolocation routing |
| Weighted routing policy | Need percentage-based traffic distribution across endpoints. | Do not use for lowest-latency routing. | 80/20 split, canary, blue-green DNS, weighted records | Latency routing, Failover routing, Route 53 health checks |
| Latency routing policy | Need users routed to the AWS Region with lowest latency. | Do not use to force users by country.<br>Use geolocation. | lowest latency, nearest Region, performance-based DNS | Geolocation, Geoproximity, Global Accelerator |
| Failover routing policy | Need active-passive DNS failover using health checks. | Do not use for active-active weighted distribution. | primary/secondary, disaster recovery, DNS failover, health check | Route 53 health checks, Multi-value answer, Global Accelerator |
| Geolocation routing policy | Need routing based on user location such as country or continent. | Do not use for latency-based routing. | route by country, continent, default location | Geoproximity, Latency routing, CloudFront geo restriction |
| Geoproximity routing policy | Need route by geographic distance with optional traffic bias. | Do not use when simple country-based routing is enough. | bias traffic, geographic distance, Route 53 traffic flow | Geolocation, Latency routing, Global Accelerator |
| IP-based routing policy | Need DNS routing based on client IP CIDR ranges. | Do not use for broad geographic routing when geolocation is enough. | client CIDR, route by source IP, ISP-specific routing | Geolocation, Geoproximity, Route 53 Resolver |
| Multi-value answer routing | Need multiple healthy IP answers and simple client-side load distribution. | Do not use as a replacement for ELB. | return multiple healthy records, DNS load balancing | Simple routing, Health checks, ELB |
| Route 53 health checks | Need DNS failover based on endpoint health. | Do not use to check private endpoints directly unless using CloudWatch alarms. | health check, endpoint monitoring, DNS failover | CloudWatch alarms, Failover routing, Multi-value answer |
| EC2 basics | Need virtual servers with full OS and instance control. | Do not choose when you want serverless compute.<br>Use Lambda or Fargate. | virtual machine, instance, AMI, user data, SSH | Lambda, ECS, EKS, Elastic Beanstalk |
| EC2 sizing and configuration | Choose CPU, memory, storage, network, AMI, and AZ based on workload. | Do not overprovision without using Auto Scaling or right sizing. | instance size, vCPU, memory, network performance | Instance types, Auto Scaling, Compute Optimizer |
| EC2 instance types | Choose family based on workload: compute, memory, storage, GPU, general purpose. | Do not pick general purpose when workload clearly needs optimized hardware. | compute optimized, memory optimized, burstable, GPU | EC2 sizing, Auto Scaling, Compute Optimizer |
| EC2 IAM roles | Need EC2 to call AWS APIs securely using temporary credentials. | Do not store access keys on the instance. | instance profile, EC2 access S3, temporary credentials | IAM roles, STS, Systems Manager |
| EC2 SSH connection | Need admin shell access to Linux EC2 instances. | Do not open SSH to the world if avoidable.<br>Use Systems Manager Session Manager. | key pair, port 22, SSH, bastion host | EC2 Key Pairs, Security Groups, Session Manager |
| EC2 purchase options | Choose pricing model based on usage pattern and flexibility. | Do not use Spot for workloads that cannot tolerate interruption. | On-Demand, Reserved, Savings Plans, Spot, Dedicated Host | EC2, Auto Scaling, Cost Explorer |
| On-Demand Instances | Need flexible compute with no long-term commitment. | Do not choose for steady long-running workloads if cost optimization is required. | unpredictable workload, no commitment, pay per second | Reserved Instances, Savings Plans, Spot |
| Reserved Instances | Need discounted pricing for steady-state EC2 or RDS usage with commitment. | Do not use for highly variable or short-lived workloads. | 1-year or 3-year commitment, steady usage, reservation | Savings Plans, On-Demand, RDS Reserved Instances |
| Savings Plans | Need flexible compute discounts across instance families, Regions, or services. | Do not choose when you need capacity reservation by itself. | compute commitment, flexible discount, EC2/Fargate/Lambda savings | Reserved Instances, On-Demand, Compute Optimizer |
| Spot Instances | Need lowest-cost compute for fault-tolerant or interruptible workloads. | Do not use for critical stateful workloads that cannot handle interruption. | interruptible, spare capacity, batch jobs, lowest cost | Spot Fleet, Auto Scaling, AWS Batch, On-Demand |
| Dedicated Hosts | Need physical server dedicated to you, often for licensing or compliance. | Do not use only for basic instance isolation.<br>Dedicated Instances may be enough. | BYOL, physical server, licensing, compliance | Dedicated Instances, EC2, License Manager |
| Dedicated Instances | Need instances running on hardware dedicated to one customer. | Do not choose when you need host-level visibility or license affinity.<br>Use Dedicated Hosts. | dedicated hardware, isolation, compliance | Dedicated Hosts, EC2 |
| Elastic IP | Need static public IPv4 address for an AWS resource. | Do not use for scalable apps behind ELB unless truly needed. | static IP, public IPv4, remap address | ENI, NAT Gateway, ALB, Global Accelerator |
| Elastic Network Interface | Need attachable virtual network card with private IPs and security groups. | Do not use as a load balancer replacement. | ENI, private IP, failover network interface, MAC address | EC2, Security Groups, Elastic IP |
| Cluster placement group | Need very low latency and high throughput between EC2 instances in one AZ. | Do not use across many AZs for high availability. | low latency, high throughput, HPC, tightly coupled | Spread placement group, Partition placement group |
| Spread placement group | Need instances placed on separate hardware to reduce correlated failure. | Do not use for very large numbers of instances per AZ. | separate hardware, high availability, small critical instances | Cluster placement group, Partition placement group |
| Partition placement group | Need large distributed workloads spread across logical partitions. | Do not use for tightly coupled low-latency HPC. | HDFS, Cassandra, Kafka, partition failure isolation | Cluster placement group, Spread placement group |
| EC2 Hibernate | Need to stop instance and preserve in-memory RAM state for faster resume. | Do not use if instance type, AMI, root volume encryption, or size is unsupported. | preserve RAM, fast startup, hibernate instance | EC2 Stop, AMI, EBS root volume |
| Golden AMI | Need preconfigured image for consistent EC2 launches. | Do not manually configure every instance after launch. | pre-baked image, standardized server, faster boot | AMI, EC2 Image Builder, Auto Scaling |
| Elastic Beanstalk | Need easy deployment and management of web apps without managing infrastructure details. | Do not choose when you need full control over every AWS resource. | deploy code, managed platform, web app, PaaS | EC2, ALB, Auto Scaling, CloudFormation |
| Elastic Load Balancers | Need distribute traffic across targets for availability and scale. | Do not use DNS routing alone when active health-aware load balancing is required. | load balancing, health checks, distribute traffic | ALB, NLB, GWLB, Target Groups |
| Application Load Balancer | Need layer 7 HTTP/HTTPS routing by host, path, headers, or query. | Do not use for ultra-low-latency TCP/UDP workloads.<br>Use NLB. | path-based routing, host-based routing, HTTP, HTTPS, containers | NLB, CloudFront, API Gateway, ECS |
| Network Load Balancer | Need layer 4 TCP/UDP/TLS, static IPs, or extreme performance. | Do not use for path-based HTTP routing.<br>Use ALB. | TCP, UDP, static IP, low latency, millions of requests | ALB, Global Accelerator, PrivateLink |
| Gateway Load Balancer | Need deploy and scale third-party virtual appliances transparently. | Do not use for normal web app traffic routing.<br>Use ALB or NLB. | firewall appliance, intrusion detection, transparent inspection | Network Firewall, Third-party appliances, GENEVE |
| Target groups | Need register EC2, IP, Lambda, or containers behind a load balancer. | Do not confuse target group with DNS routing policy. | registered targets, health checks, ALB target, ECS service | ALB, NLB, Auto Scaling, ECS |
| Sticky sessions | Need same client routed to same target for session affinity. | Do not use when app can store session state externally.<br>Prefer stateless design. | session affinity, stickiness, same backend | ALB, ElastiCache, DynamoDB, Cookies |
| Cross-zone load balancing | Need load balancer to distribute traffic evenly across targets in all enabled AZs. | Do not ignore inter-AZ data charges for some load balancer types. | distribute across AZs, uneven targets, cross-zone | ALB, NLB, Auto Scaling |
| Connection draining | Need existing requests to finish before deregistering or terminating targets. | Do not terminate immediately when graceful shutdown is required. | deregistration delay, draining connections, graceful shutdown | ALB, NLB, Auto Scaling |
| Auto Scaling Groups | Need automatically add/remove EC2 instances based on demand or health. | Do not use for databases that need managed scaling differently. | scale out, scale in, desired capacity, health check replacement | EC2, Launch Template, ALB, CloudWatch |
| Scaling policies | Need control how ASG scales based on metrics or schedules. | Do not use manual scaling when demand changes automatically. | target tracking, step scaling, scheduled scaling, CPU target | ASG, CloudWatch alarms, Predictive Scaling |
| Scaling cooldown | Need pause between scaling actions to stabilize capacity. | Do not set too long when fast scale-out is required. | cooldown period, wait before scaling again, stabilization | ASG, Scaling policies, CloudWatch |
| Amazon S3 | Need highly durable object storage for files, backups, logs, static content, or data lake. | Do not use as a block device for EC2.<br>Use EBS. | object storage, bucket, 11 nines durability, unlimited storage | EBS, EFS, Glacier, CloudFront |
| S3 lifecycle policies | Need automatic transition or expiration of objects to reduce cost. | Do not use when objects need immediate frequent access forever. | transition to IA, archive to Glacier, expire old objects | S3 storage classes, Glacier, Intelligent-Tiering |
| S3 Standard | Need frequent access, low latency, high durability across multiple AZs. | Do not use for rarely accessed data where IA or Glacier is cheaper. | frequently accessed, low latency, default storage class | Standard-IA, Intelligent-Tiering, One Zone-IA |
| S3 Intelligent-Tiering | Need automatic cost optimization for data with unknown or changing access patterns. | Do not use for tiny short-lived objects if monitoring overhead matters. | unknown access pattern, automatic tiering, cost optimization | S3 Standard, Standard-IA, Lifecycle policies |
| S3 Standard-IA | Need infrequently accessed data that still needs milliseconds access and multi-AZ durability. | Do not use for frequently accessed data due to retrieval fees. | infrequent access, milliseconds retrieval, multi-AZ | S3 Standard, One Zone-IA, Glacier Instant Retrieval |
| S3 One Zone-IA | Need cheaper infrequent access and can tolerate loss of one AZ. | Do not use for critical data requiring multi-AZ resilience. | non-critical data, re-creatable data, single AZ storage | Standard-IA, S3 Standard, Glacier |
| S3 Glacier classes | Need low-cost archive storage with retrieval from instant to hours. | Do not use for hot data needing frequent immediate reads. | archive, long-term retention, retrieval time, cheapest storage | S3 Lifecycle, Glacier Instant, Flexible, Deep Archive |
| S3 versioning | Need keep multiple versions and recover from accidental deletion/overwrite. | Do not use alone for compliance immutability.<br>Use Object Lock too. | previous versions, delete marker, recover object | S3 Object Lock, MFA Delete, Replication |
| S3 replication | Need copy objects automatically to another bucket, account, or Region. | Do not assume existing objects are copied unless using batch replication. | replicate objects, destination bucket, same account or cross-account | CRR, SRR, Batch Replication, Versioning |
| Cross-Region Replication | Need automatic object copy to another AWS Region for compliance, latency, or DR. | Do not use when same-Region copy is required.<br>Use SRR. | cross-Region copy, disaster recovery, compliance Region | S3 Replication, SRR, KMS |
| Same-Region Replication | Need object copy within same Region for logs, compliance, or account separation. | Do not use for geographic disaster recovery across Regions. | same Region copy, account separation, compliance copy | CRR, S3 Replication, Batch Operations |
| Live replication | Need new S3 objects replicated automatically after rule is enabled. | Do not expect old existing objects to replicate automatically. | new objects, automatic replication, replication rule | S3 Replication, On-demand replication, Batch Operations |
| On-demand replication | Need replicate existing S3 objects after the fact. | Do not use when only new objects need replication.<br>Use live replication. | existing objects, batch replication, backfill replication | S3 Batch Operations, S3 Replication |
| S3 Batch Operations | Need perform large-scale actions on many existing S3 objects. | Do not use for one-off small object operations. | millions of objects, bulk copy, bulk tag, batch replication | S3 Inventory, Lambda, S3 Replication |
| S3 static website hosting | Need host static HTML/CSS/JS website directly from S3. | Do not use for HTTPS directly on S3 website endpoint.<br>Use CloudFront for HTTPS. | static website, index document, website endpoint | CloudFront, Route 53, ACM |
| S3 CORS | Need browser-based web app from one domain to access S3 resources in another domain. | Do not use CORS for server-to-server authorization. | cross-origin request, browser error, Access-Control-Allow-Origin | S3 bucket policy, CloudFront, API Gateway |
| S3 MFA Delete | Need MFA required to permanently delete object versions or suspend versioning. | Do not expect it to work without versioning and root-level setup. | MFA required, delete version, suspend versioning | S3 Versioning, Object Lock |
| S3 access logs | Need record requests made to an S3 bucket for audit/troubleshooting. | Do not use for API activity across AWS.<br>Use CloudTrail. | bucket access logs, requester, object request logging | CloudTrail, CloudWatch Logs, S3 Server Access Logging |
| Requester Pays | Need requester to pay S3 request and data transfer costs. | Do not use for normal private app storage where owner pays. | requester pays, data sharing, requester charged | S3, Athena public datasets |
| S3 Event Notifications | Need trigger actions when S3 object events occur. | Do not use for complex event routing across many apps.<br>Use EventBridge. | object created, object deleted, trigger Lambda | Lambda, SQS, SNS, EventBridge |
| S3 Storage Lens | Need organization-wide visibility into S3 usage, activity, and cost optimization. | Do not use for real-time object-level access logs. | storage analytics, bucket metrics, organization visibility | S3 Analytics, CloudWatch, Cost Explorer |
| EBS | Need block storage volume for EC2 with persistence beyond instance stop. | Do not use as shared file system for many instances.<br>Use EFS. | block storage, attach to EC2, persistent volume | EC2, EFS, Instance Store, Snapshots |
| EBS snapshots | Need point-in-time backup of EBS volumes stored in S3-managed storage. | Do not use as live shared storage. | point-in-time backup, incremental snapshot, restore volume | EBS, AMI, AWS Backup |
| EBS snapshot features | Use fast snapshot restore, archive, copy, and lifecycle management for snapshot optimization. | Do not assume archived snapshots restore instantly. | FSR, snapshot archive, copy snapshot, DLM | EBS Snapshots, AWS Backup, AMI |
| EBS volume types | Choose gp3/gp2 for general purpose, io1/io2 for high IOPS, st1/sc1 for throughput HDD. | Do not use HDD volumes as boot volumes. | gp3, io2, high IOPS, throughput optimized, cold HDD | EBS, EC2, RDS storage |
| EBS Multi-Attach | Need attach one io1/io2 EBS volume to multiple EC2 instances in same AZ. | Do not use across AZs or with non-cluster-aware file systems. | shared block volume, same AZ, clustered app | EFS, FSx, EBS io2 |
| Amazon Machine Image | Need template to launch EC2 instances with OS and configuration. | Do not use AMI as a data backup replacement for all app data. | AMI, launch template, golden image, image backup | EC2, EBS snapshots, Image Builder |
| Instance Store | Need temporary high-performance local storage physically attached to host. | Do not use for data that must survive stop, terminate, or host failure. | ephemeral storage, temporary data, high IOPS local disk | EBS, EFS, EC2 |
| EFS | Need managed shared NFS file system for Linux instances across AZs. | Do not use for Windows file shares.<br>Use FSx for Windows. | shared file system, NFS, multiple EC2, Linux | EBS, FSx, S3, ECS volumes |
| EFS use cases | Choose for shared web content, home directories, containers, and Linux app shared storage. | Do not use for low-latency block storage.<br>Use EBS. | shared web files, lift-and-shift NFS, multi-AZ file system | EBS, FSx, S3 |
| EFS performance modes | General Purpose for latency-sensitive apps.<br>Max I/O for massive parallel access. | Do not choose Max I/O when lowest latency is required. | General Purpose, Max I/O, latency, parallel workloads | EFS throughput modes, FSx |
| EFS throughput modes | Bursting for variable workloads, Provisioned or Elastic for higher predictable throughput. | Do not overpay for provisioned throughput if workload can burst. | bursting throughput, provisioned throughput, elastic throughput | EFS performance modes, CloudWatch |
| EFS storage classes | Use Standard for frequent multi-AZ access, IA/Archive for colder data, One Zone for lower-cost single-AZ. | Do not use One Zone for critical multi-AZ resilience. | EFS Standard, EFS IA, One Zone, lifecycle | EFS Lifecycle, S3, FSx |
| EBS vs EFS vs Instance Store | EBS = single-instance block.<br>EFS = shared managed file.<br>Instance Store = temporary local disk. | Do not choose Instance Store for persistent data or EBS for multi-AZ shared files. | block vs file vs ephemeral, shared storage, persistent storage | EBS, EFS, Instance Store, FSx |
| Amazon FSx | Need managed high-performance file systems like Windows, Lustre, NetApp ONTAP, or OpenZFS. | Do not use EFS for Windows SMB shares. | Windows file share, Lustre HPC, SMB, high-performance file system | EFS, EBS, S3, Storage Gateway |
| AWS Storage Gateway | Need hybrid on-premises access to AWS storage. | Do not use for high-speed one-time migration only.<br>Use DataSync or Snowball. | hybrid storage, on-prem cache, file gateway, tape replacement | DataSync, Snowball, S3, FSx |
| File Gateway | Need on-premises NFS/SMB access backed by S3. | Do not use for block volume backups.<br>Use Volume Gateway. | NFS to S3, SMB to S3, file share, on-prem cache | Storage Gateway, S3, FSx File Gateway |
| Volume Gateway | Need on-premises iSCSI block storage backed by AWS. | Do not use for object-style file shares.<br>Use File Gateway. | iSCSI, cached volumes, stored volumes, block storage | Storage Gateway, EBS snapshots |
| Tape Gateway | Need replace physical tape backups with virtual tapes in AWS. | Do not use for active file sharing. | virtual tape library, VTL, backup archive, tape replacement | Storage Gateway, S3 Glacier, Backup |
| AWS DataSync | Need fast online data transfer between on-premises storage and AWS storage. | Do not use for offline petabyte migration with limited bandwidth.<br>Use Snowball. | online migration, NFS/SMB to S3, transfer acceleration, scheduled sync | Storage Gateway, Snowball, Transfer Family |
| AWS Transfer Family | Need managed SFTP, FTPS, or FTP access to S3 or EFS. | Do not use for general VPN file sharing. | SFTP to S3, managed FTP, partner file transfer | S3, EFS, DataSync |
| AWS Snowball | Need offline large-scale data transfer or edge compute in disconnected locations. | Do not use for small data transfers over good network links. | offline migration, petabytes, rugged device, limited bandwidth | DataSync, Snowmobile, Storage Gateway |
| Snowball into Glacier | Need import large archive datasets to AWS, then lifecycle or store in Glacier classes. | Do not assume Snowball writes directly to Glacier in every workflow; often import to S3 first then transition. | archive import, offline to Glacier, lifecycle to Glacier | Snowball, S3, S3 Glacier, Lifecycle policies |
| Amazon RDS | Need managed relational database like MySQL, PostgreSQL, MariaDB, Oracle, or SQL Server. | Do not choose for NoSQL key-value workloads.<br>Use DynamoDB. | managed relational DB, SQL database, automated backups | Aurora, DynamoDB, EC2 database, Redshift |
| RDS Multi-AZ | Need high availability and automatic failover for RDS. | Do not choose for read scaling.<br>Use read replicas. | standby database, automatic failover, high availability | RDS Read Replicas, Aurora Replicas |
| RDS read replicas | Need scale read traffic or create read-only copies. | Do not use as primary HA failover solution unless promoted manually. | read scaling, read-heavy workload, reporting replica | RDS Multi-AZ, Aurora Replicas |
| RDS backups | Need automated backups, point-in-time restore, snapshots. | Do not rely only on read replicas for backup. | automated backup, PITR, retention period, DB snapshot | AWS Backup, RDS Snapshots, Aurora backups |
| RDS monitoring | Need database metrics, logs, performance insights, and enhanced monitoring. | Do not use CloudTrail for query performance metrics. | Performance Insights, Enhanced Monitoring, slow query logs | CloudWatch, RDS Events, CloudTrail |
| RDS security | Use security groups, encryption, IAM auth, KMS, SSL/TLS, and private subnets. | Do not put databases publicly accessible unless specifically required. | private DB, encryption at rest, IAM auth, security group | KMS, Secrets Manager, VPC, IAM |
| RDS Proxy | Need connection pooling and better failover handling for Lambda or highly concurrent apps. | Do not use when app has few stable DB connections. | connection pooling, Lambda to RDS, reduce DB connections | RDS, Aurora, Lambda, Secrets Manager |
| Amazon Aurora | Need cloud-optimized relational DB with high performance, replication, and managed storage. | Do not choose when standard RDS engine is enough and cost is priority. | Aurora MySQL, Aurora PostgreSQL, 6 copies, fast failover | RDS, Aurora Global Database, DynamoDB |
| Aurora backups | Need continuous backup to S3 and point-in-time restore for Aurora cluster. | Do not manage backup storage manually like EC2 database. | continuous backup, PITR, cluster snapshot | RDS backups, AWS Backup, S3 |
| Aurora security | Use VPC isolation, security groups, KMS encryption, IAM auth, TLS, and Secrets Manager. | Do not expose Aurora publicly unless required. | encrypted Aurora, IAM database authentication, TLS | KMS, Secrets Manager, RDS security |
| Aurora global database concept | Need low-latency global reads and fast cross-Region disaster recovery. | Do not choose for simple single-Region HA.<br>Use Multi-AZ or replicas. | global database, cross-Region replicas, low RPO, global reads | Aurora Replicas, RDS Read Replicas, DynamoDB Global Tables |
| RDS MySQL to Aurora MySQL migration | Need migrate MySQL-compatible RDS to Aurora for performance/scaling. | Do not choose if engine compatibility is not acceptable. | Aurora read replica migration, snapshot restore, MySQL compatible | DMS, RDS Snapshot, Aurora MySQL |
| Amazon DynamoDB | Need serverless NoSQL key-value/document database with single-digit millisecond performance. | Do not choose for complex joins and relational SQL queries. | key-value, NoSQL, serverless database, high scale | RDS, Aurora, DAX, ElastiCache |
| DynamoDB Accelerator / DAX | Need microsecond read caching for DynamoDB. | Do not use for generic database cache or session cache.<br>Use ElastiCache. | DAX, DynamoDB cache, microsecond reads | DynamoDB, ElastiCache, CloudFront |
| DAX vs ElastiCache | Choose DAX for DynamoDB API-compatible caching.<br>Choose ElastiCache for general Redis/Memcached caching. | Do not use DAX with RDS or generic app cache. | DynamoDB cache vs Redis cache, microsecond reads, session cache | DynamoDB, DAX, ElastiCache |
| DynamoDB Streams | Need capture item-level changes from DynamoDB table. | Do not use for high-throughput custom streaming from many producers.<br>Use Kinesis Data Streams. | table change events, item modified, trigger Lambda | Lambda, Kinesis Data Streams, EventBridge |
| DynamoDB Streams vs Kinesis Data Streams | DynamoDB Streams = table change log.<br>Kinesis Data Streams = general real-time streaming platform. | Do not use DynamoDB Streams for non-DynamoDB producers. | table changes vs event stream, ordered shard records | DynamoDB, Lambda, Kinesis |
| Amazon ElastiCache | Need in-memory caching with Redis or Memcached. | Do not use as durable primary database. | cache, Redis, Memcached, session store, sub-millisecond | DAX, RDS, DynamoDB, CloudFront |
| ElastiCache security | Use VPC, security groups, encryption in transit/at rest, Redis AUTH/IAM where supported. | Do not expose cache publicly. | private cache, Redis AUTH, TLS, security group | VPC, KMS, Secrets Manager |
| Amazon DocumentDB | Need MongoDB-compatible managed document database. | Do not choose for native MongoDB features not supported or relational SQL. | MongoDB compatible, JSON documents, managed document DB | DynamoDB, MongoDB on EC2, Neptune |
| Amazon Neptune | Need managed graph database for highly connected data. | Do not use for simple key-value or relational workloads. | graph database, relationships, Gremlin, SPARQL, social graph | DynamoDB, RDS, DocumentDB |
| Amazon Redshift | Need managed data warehouse for analytics using SQL at scale. | Do not use for OLTP transactional apps.<br>Use RDS or Aurora. | data warehouse, OLAP, analytics SQL, columnar storage | Athena, EMR, RDS, QuickSight |
| Amazon S3 as a data store | Need durable, cheap object storage for data lake, logs, backups, or analytics input. | Do not use for transactional database operations or block storage. | data lake, raw data, log storage, object store | Athena, Glue, Lake Formation, Redshift Spectrum |
| AWS Lambda | Need serverless functions for event-driven short-running code. | Do not use for long-running processes over Lambda limits or full server control. | serverless function, event-driven, no servers, pay per request | ECS Fargate, EC2, Step Functions |
| Lambda concurrency errors | Occur when requests exceed available concurrency or throttling limits. | Do not fix only with retries if capacity is the root issue. | throttling, TooManyRequestsException, concurrency limit | Reserved concurrency, Provisioned concurrency, SQS |
| Reserved concurrency | Need guarantee and cap Lambda concurrency for a function. | Do not use when you need to reduce cold starts.<br>Use provisioned concurrency. | limit concurrency, reserve capacity, protect downstream DB | Provisioned concurrency, Lambda throttling |
| Provisioned concurrency | Need pre-initialized Lambda environments to reduce cold starts. | Do not use only to cap usage.<br>Use reserved concurrency. | reduce cold start, pre-warmed Lambda, latency-sensitive | Reserved concurrency, SnapStart |
| Lambda cold starts | Problem when new execution environments add startup latency. | Do not assume all Lambda calls have cold starts. | startup latency, VPC cold start, first invocation delay | Provisioned concurrency, SnapStart, CloudFront Functions |
| Lambda SnapStart | Need faster startup for supported Java Lambda functions. | Do not choose for all runtimes; support is limited. | Java cold start, snapshot initialized function | Provisioned concurrency, Lambda |
| Lambda@Edge | Need run code at CloudFront edge locations for request/response customization. | Do not use for ultra-lightweight header/URL changes when CloudFront Functions is enough. | edge compute, viewer request, origin request, CloudFront trigger | CloudFront Functions, CloudFront, Lambda |
| CloudFront Functions | Need lightweight JavaScript at edge for viewer request/response manipulation. | Do not use for network access, file system access, or long-running code.<br>Use Lambda@Edge. | lightweight edge function, header rewrite, URL redirect | Lambda@Edge, CloudFront |
| Amazon API Gateway | Need managed API front door for REST, HTTP, or WebSocket APIs. | Do not use when simple ALB routing is enough and API features are not needed. | REST API, HTTP API, WebSocket, throttling, API keys | Lambda, ALB, CloudFront, Cognito |
| API Gateway endpoint types | Edge-optimized for global clients, Regional for same Region/custom CloudFront, Private for VPC-only APIs. | Do not choose public endpoint for private internal APIs. | edge-optimized, regional endpoint, private API | API Gateway, CloudFront, VPC Endpoint |
| API Gateway security | Use IAM, Lambda authorizers, Cognito, resource policies, throttling, and WAF. | Do not rely only on API keys for authentication. | authorizer, Cognito auth, IAM auth, API throttling | Cognito, WAF, Lambda authorizer, IAM |
| AWS Step Functions | Need orchestrate workflows across Lambda, ECS, API calls, and human/business steps. | Do not use for simple one-function event processing. | workflow, state machine, retries, error handling, orchestration | Lambda, EventBridge, SQS, SWF |
| Amazon Cognito | Need user sign-up, sign-in, identity federation, and app authentication. | Do not use IAM users for application end users. | user pool, identity pool, social login, app users | API Gateway, IAM, Identity Center, ALB auth |
| Amazon SQS | Need decouple apps with durable message queues. | Do not use for pub/sub fanout alone.<br>Use SNS plus SQS. | queue, decouple, buffer, message processing, async | SNS, EventBridge, Kinesis, Amazon MQ |
| SQS visibility timeout | Need hide message while consumer processes it. | Do not set shorter than processing time or duplicate processing may happen. | message invisible, processing timeout, duplicate message | SQS, DLQ, Lambda event source |
| SQS long polling | Need reduce empty responses and cost by waiting for messages. | Do not use short polling when queues are often empty and cost matters. | wait time, reduce empty receives, long poll | SQS, Lambda poller |
| SQS FIFO | Need ordered and exactly-once processing semantics within message groups. | Do not choose FIFO when maximum throughput and order does not matter. | first in first out, deduplication, message group ID, ordering | SQS Standard, SNS FIFO, Kinesis |
| SQS security | Use IAM policies, SSE-KMS, VPC endpoints, and queue resource policies. | Do not expose queues publicly unless explicitly intended. | encrypted queue, queue policy, private access | IAM, KMS, VPC Endpoints, SNS |
| SQS consumer autoscaling | Scale consumers based on queue depth or age of messages. | Do not scale only on CPU when backlog is the real signal. | queue depth, approximate number of messages, backlog | ASG, ECS Service Auto Scaling, Lambda, CloudWatch |
| Amazon SNS | Need pub/sub notifications and fanout to multiple subscribers. | Do not use for durable queue processing alone.<br>Use SQS. | topic, publish-subscribe, fanout, push notification | SQS, EventBridge, SES, Pinpoint |
| SNS security | Use IAM, topic policies, encryption, and private access where needed. | Do not allow unrestricted publishing or subscribing. | topic policy, encrypted topic, cross-account publish | IAM, KMS, SQS, VPC Endpoints |
| SNS + SQS fanout | Need one message delivered to multiple durable queues independently. | Do not send directly to one SQS queue if many consumers need independent processing. | fanout, multiple subscribers, decoupled consumers | SNS, SQS, EventBridge |
| Amazon Kinesis Data Streams | Need real-time streaming data ingestion and custom consumers. | Do not use when you only need simple queue decoupling.<br>Use SQS. | real-time stream, shards, ordered records, producers/consumers | Firehose, MSK, SQS, DynamoDB Streams |
| Amazon Data Firehose | Need fully managed delivery of streaming data to S3, Redshift, OpenSearch, or third-party destinations. | Do not use when custom low-latency stream consumers are required.<br>Use Kinesis Data Streams. | load streaming data, delivery stream, near real-time delivery | Kinesis Data Streams, S3, Redshift, OpenSearch |
| Firehose vs Kinesis Data Streams | Firehose = managed delivery.<br>Kinesis Data Streams = custom real-time processing. | Do not use Firehose for multiple custom consumers with shard control. | delivery stream vs data stream, managed loading vs custom processing | Kinesis, Firehose, Lambda, S3 |
| SQS vs SNS vs Kinesis | SQS = queue.<br>SNS = pub/sub fanout.<br>Kinesis = real-time ordered streaming. | Do not use SNS as a queue or SQS as a streaming analytics platform. | queue vs topic vs stream, decouple, fanout, real-time | EventBridge, Amazon MQ, Firehose |
| Amazon MQ | Need managed ActiveMQ or RabbitMQ for existing message broker apps. | Do not choose for cloud-native unlimited scale queue.<br>Use SQS/SNS. | migrate broker, JMS, AMQP, RabbitMQ, ActiveMQ | SQS, SNS, EventBridge |
| Amazon EventBridge | Need event bus for SaaS, AWS service events, custom app events, and routing rules. | Do not use for high-throughput ordered streaming.<br>Use Kinesis. | event bus, event-driven architecture, SaaS events, scheduled rule | SNS, SQS, Step Functions, CloudTrail |
| EventBridge rules | Need match event patterns or schedules and route to targets. | Do not use when simple direct S3 notification is enough. | event pattern, schedule, cron, target | EventBridge Bus, Lambda, Step Functions |
| EventBridge schema registry | Need discover and manage event schemas for event-driven apps. | Do not use for database table schema management. | event schema, discover events, code bindings | EventBridge, Glue Schema Registry |
| EventBridge resource-based policies | Need allow cross-account event publishing to an event bus. | Do not use IAM identity policy alone when resource-side permission is needed. | cross-account event bus, allow account to put events | EventBridge, IAM, Organizations |
| Amazon CloudFront | Need CDN to cache and deliver content globally with low latency. | Do not use for dynamic TCP acceleration.<br>Use Global Accelerator. | CDN, edge cache, global content delivery, cache behavior | S3, ALB, ACM, WAF, Global Accelerator |
| CloudFront geo restriction | Need allow or block content by country. | Do not use for DNS routing by location.<br>Use Route 53 geolocation. | block country, whitelist country, geographic restriction | WAF, Route 53 Geolocation, CloudFront |
| CloudFront cache invalidation | Need remove or refresh cached objects before TTL expires. | Do not invalidate frequently if versioned file names can solve it cheaper. | clear cache, invalidate object, refresh CDN | CloudFront TTL, S3 versioned object names |
| CloudFront with S3 | Need fast global delivery of S3 objects, often private origin with OAC/OAI. | Do not make S3 public when private CloudFront access is required. | S3 origin, OAC, OAI, static content CDN | S3, ACM, Route 53, WAF |
| CloudFront with ALB | Need global caching and edge security in front of dynamic web app on ALB. | Do not use CloudFront if only regional load balancing is required. | ALB origin, dynamic content, global edge cache | ALB, WAF, ACM, Route 53 |
| AWS Global Accelerator | Need improve availability/performance for TCP/UDP apps using static anycast IPs. | Do not use mainly for content caching.<br>Use CloudFront. | static anycast IP, TCP acceleration, global traffic, fast failover | CloudFront, Route 53, ALB, NLB |
| Global Accelerator vs CloudFront | Global Accelerator = network acceleration for TCP/UDP, static IPs.<br>CloudFront = CDN caching for HTTP content. | Do not choose Global Accelerator for object caching or CloudFront for non-HTTP TCP acceleration. | static IP vs cache, TCP/UDP vs CDN | Route 53, ALB, NLB |
| Route 53 vs Global Accelerator vs CloudFront | Route 53 = DNS routing.<br>Global Accelerator = anycast IP acceleration.<br>CloudFront = CDN cache. | Do not use DNS alone for instant failover or caching. | DNS vs anycast vs CDN, routing vs acceleration vs caching | Route 53, Global Accelerator, CloudFront |
| AWS Container Management | Need run, store, orchestrate, and scale containers on AWS. | Do not use containers when simple serverless function is enough. | containers, image registry, orchestration, tasks, pods | ECS, EKS, ECR, Fargate, App Runner |
| Amazon ECS | Need AWS-native container orchestration with tasks and services. | Do not choose when Kubernetes API/ecosystem is required.<br>Use EKS. | containers, task definition, ECS service, cluster | EKS, Fargate, ECR, App Runner |
| ECS EC2 launch type | Need containers on self-managed EC2 capacity with more host control. | Do not use when you want serverless containers.<br>Use Fargate. | EC2 launch type, manage instances, ECS capacity provider | ECS Fargate, Auto Scaling, EC2 |
| ECS Fargate launch type | Need serverless containers without managing EC2 hosts. | Do not use when you need host-level access or special EC2 configuration. | serverless containers, no EC2 management, task CPU/memory | ECS EC2, EKS Fargate, App Runner |
| ECS IAM roles | Use task role for app AWS API permissions.<br>Use execution role for pulling images/logging. | Do not give container permissions through EC2 instance role when task role is needed. | task role, execution role, pull image, write logs | IAM, ECR, CloudWatch Logs |
| ECS load balancer integration | Need expose ECS services through ALB or NLB with health checks. | Do not expose tasks directly when load balancing and service discovery are needed. | ECS service behind ALB, dynamic port mapping, target group | ALB, NLB, Target Groups, Service Discovery |
| ECS data volumes with EFS | Need persistent shared storage for ECS tasks. | Do not use container local storage for data that must persist. | ECS persistent volume, shared files, EFS mount | EFS, ECS, Fargate |
| ECS Auto Scaling | Need scale ECS tasks based on demand or custom metrics. | Do not scale only EC2 hosts if service task count is the bottleneck. | scale ECS service, target tracking, CPU/memory, request count | Application Auto Scaling, CloudWatch, ASG |
| Amazon ECR | Need private container image registry for AWS workloads. | Do not use S3 as a container image registry. | Docker image registry, push image, pull image, scan images | ECS, EKS, App Runner, Inspector |
| Amazon EKS | Need managed Kubernetes control plane on AWS. | Do not choose if you do not need Kubernetes complexity.<br>Use ECS or App Runner. | Kubernetes, pods, nodes, managed control plane | ECS, Fargate, ECR, Karpenter |
| EKS node types | Choose managed node groups, self-managed nodes, Fargate, or hybrid nodes based on control needs. | Do not use Fargate when privileged pods or daemonsets are required. | managed node group, Fargate pod, self-managed node | EKS, EC2, Fargate, Auto Scaling |
| EKS data volumes | Need persistent storage for Kubernetes pods using EBS, EFS, or FSx CSI drivers. | Do not use emptyDir for persistent data. | CSI driver, persistent volume, EBS volume, EFS shared storage | EBS, EFS, FSx, Kubernetes PV/PVC |
| AWS App Runner | Need simple fully managed container or source-code web app deployment. | Do not use when deep VPC/container orchestration control is needed. | simple container web app, build and deploy, managed HTTPS | ECS, Elastic Beanstalk, Amplify |
| AWS App2Container | Need help containerize existing Java or .NET applications. | Do not use for brand-new cloud-native apps that are already containerized. | app modernization, containerize legacy app, Java .NET | ECS, EKS, App Runner |
| Amazon CloudWatch metrics | Need collect and monitor numeric performance data. | Do not use for API audit trail.<br>Use CloudTrail. | CPUUtilization, metric, namespace, custom metric | CloudWatch Alarms, Logs, Dashboards |
| CloudWatch alarms | Need trigger action when metric crosses threshold. | Do not use for full workflow orchestration.<br>Use Step Functions/EventBridge. | threshold alarm, CPU high, notify, Auto Scaling trigger | SNS, Auto Scaling, CloudWatch Metrics |
| CloudWatch logs | Need collect, store, and search application/system logs. | Do not use for object storage analytics at lowest cost long term without export. | log group, log stream, application logs | Logs Insights, S3 export, Lambda, EC2 agent |
| CloudWatch Logs Insights | Need query and analyze CloudWatch Logs interactively. | Do not use for SQL queries over S3 data.<br>Use Athena. | search logs, query logs, filter errors | CloudWatch Logs, Athena, OpenSearch |
| CloudWatch Logs S3 export | Need archive or analyze CloudWatch logs in S3. | Do not use for real-time streaming.<br>Use subscriptions. | export logs to S3, log archive, Athena analysis | S3, Athena, Logs subscriptions |
| CloudWatch Logs subscriptions | Need stream log events in near real time to Lambda, Kinesis, or Firehose. | Do not use for one-time historical export.<br>Use S3 export. | subscription filter, real-time log processing | Lambda, Kinesis Data Streams, Firehose |
| CloudWatch Logs aggregation across accounts and Regions | Need centralize logs from many accounts/Regions for operations. | Do not manually check each account when central observability is required. | centralized logging, cross-account logs, organization logs | CloudWatch Logs, Organizations, Kinesis, S3 |
| CloudWatch logs for EC2 | Need OS/application logs from EC2 sent to CloudWatch using agent. | Do not expect EC2 logs to appear automatically without agent/config. | CloudWatch agent, system logs, application logs, EC2 logs | EC2, Systems Manager, CloudWatch Logs |
| CloudWatch Container Insights | Need observability for ECS, EKS, and Kubernetes containers. | Do not use for Lambda-specific deep metrics.<br>Use Lambda Insights. | container metrics, pod metrics, task metrics | ECS, EKS, CloudWatch, Lambda Insights |
| CloudWatch Lambda Insights | Need enhanced Lambda performance metrics and troubleshooting. | Do not use for container cluster metrics. | Lambda memory, duration, cold start, enhanced metrics | Lambda, CloudWatch Logs, X-Ray |
| CloudWatch Contributor Insights | Need identify top contributors to traffic, errors, or throttling from logs/metrics. | Do not use for general dashboarding only. | top N contributors, heavy hitters, throttled keys | CloudWatch Logs, DynamoDB, VPC Flow Logs |
| CloudWatch Application Insights | Need automated app monitoring and issue detection for supported apps. | Do not use as replacement for CloudTrail auditing. | application health, automated detection, app monitoring | CloudWatch, X-Ray, Systems Manager |
| CloudWatch Network Synthetic Monitor | Need monitor network health/performance for hybrid AWS/on-prem paths. | Do not use for web page synthetic canaries.<br>Use CloudWatch Synthetics. | network path monitoring, hybrid network health | CloudWatch, Network Manager, VPC Flow Logs |
| AWS CloudTrail | Need audit who did what in AWS API calls. | Do not use for CPU or app performance metrics.<br>Use CloudWatch. | API audit, user activity, management events, who deleted resource | CloudWatch, AWS Config, S3 |
| CloudTrail events | Management events track control plane.<br>Data events track resource-level activity.<br>Insights detect unusual API activity. | Do not enable all data events without considering cost. | management event, data event, S3 object access, Lambda invoke | CloudTrail, CloudTrail Insights, EventBridge |
| CloudTrail Insights | Need detect unusual API activity patterns. | Do not use for network threat detection.<br>Use GuardDuty. | unusual API calls, spike in activity, anomaly detection | CloudTrail, GuardDuty, CloudWatch |
| CloudTrail event retention | Event history keeps recent management events; trails can deliver to S3 for long-term retention. | Do not rely only on default event history for compliance retention. | 90-day event history, long-term audit, S3 trail | S3, CloudWatch Logs, Lake |
| AWS Config | Need record resource configuration history and compliance. | Do not use for API call audit trail.<br>Use CloudTrail. | configuration history, compliance, resource changes | CloudTrail, CloudWatch, Config Rules |
| AWS Config rules | Need evaluate resources against compliance rules. | Do not use for real-time network blocking. | compliant/noncompliant, required tags, encrypted volumes | Config, Remediation, Security Hub |
| AWS Config resources | Need track supported AWS resource configurations and relationships. | Do not expect every possible runtime metric to be tracked. | resource inventory, configuration item, relationship | Config, Resource Explorer, CloudTrail |
| AWS Config remediations | Need automatic or manual fix for noncompliant resources. | Do not use when human approval is required unless configured accordingly. | auto remediation, SSM automation, fix noncompliance | Config Rules, Systems Manager Automation |
| AWS Config notifications | Need notify on configuration changes or compliance changes. | Do not use for metric threshold notifications.<br>Use CloudWatch alarms. | configuration change alert, compliance notification | SNS, EventBridge, Config Rules |
| CloudWatch vs CloudTrail vs Config | CloudWatch = metrics/logs/alarms.<br>CloudTrail = API audit.<br>Config = resource configuration/compliance. | Do not use CloudWatch to answer who changed a resource; use CloudTrail/Config. | monitor vs audit vs compliance, metrics vs API calls vs config | CloudWatch, CloudTrail, AWS Config |
| AWS Trusted Advisor | Need best-practice checks for cost, security, performance, fault tolerance, and service limits. | Do not use as real-time monitoring replacement. | service limits, cost optimization, best practice checks | Well-Architected Tool, Compute Optimizer, Cost Explorer |
| AWS Well-Architected Tool | Need review workload architecture against AWS best practices. | Do not use for automated cost anomaly alerts. | architectural review, pillars, best practices | Trusted Advisor, Cost Explorer, Security Hub |
| RPO and RTO | Need define disaster recovery data loss and recovery time targets. | Do not confuse RPO with RTO.<br>RPO = data loss, RTO = downtime. | recovery point objective, recovery time objective, data loss, downtime | Backup/Restore, Pilot Light, Warm Standby, Multi-site |
| Backup and restore | Need lowest-cost DR with backups restored after disaster. | Do not choose when very low RTO is required. | lowest cost DR, restore from backup, hours to recover | AWS Backup, Pilot Light, Warm Standby |
| Pilot light | Need minimal core infrastructure always running for faster DR than backup/restore. | Do not choose when app must be fully active immediately. | minimal environment, core services running, scale after disaster | Backup/Restore, Warm Standby, Multi-site |
| Warm standby | Need scaled-down working DR environment ready to scale up quickly. | Do not choose when zero downtime active-active is required. | scaled-down environment, faster recovery, partially running | Pilot Light, Multi-site, Auto Scaling |
| Multi-site / hot site | Need active-active or near-zero downtime DR across sites/Regions. | Do not choose when cost must be minimal. | active-active, hot standby, zero downtime, immediate failover | Route 53 Failover, Global Accelerator, Aurora Global |
| AWS Elastic Disaster Recovery | Need block-level replication and fast recovery of servers to AWS. | Do not use only for database schema migration.<br>Use DMS. | server disaster recovery, continuous replication, failback | Application Migration Service, AWS Backup |
| AWS Backup | Need centralized backup policies across AWS services and accounts. | Do not use for app-level real-time replication. | centralized backup, backup vault, retention policy, cross-account backup | EBS Snapshots, RDS Backups, Backup Vault Lock |
| AWS Application Migration Service | Need lift-and-shift server migration to AWS with minimal downtime. | Do not use for ongoing DR only when Elastic Disaster Recovery is better fit. | migrate servers, lift and shift, rehost, block replication | Elastic Disaster Recovery, DMS, Migration Hub |
| AWS DMS | Need migrate databases to AWS with minimal downtime and replication. | Do not use for server OS migration.<br>Use Application Migration Service. | database migration, homogeneous, heterogeneous, CDC | SCT, RDS, Aurora, Redshift |
| On-premises strategy with AWS | Choose VPN, Direct Connect, Storage Gateway, DataSync, Outposts, or migration tools based on hybrid need. | Do not pick one service for all hybrid requirements. | hybrid cloud, on-premises connection, migration, edge | VPN, Direct Connect, Outposts, Storage Gateway |
| VMware Cloud on AWS | Need run VMware workloads on AWS with familiar VMware tools. | Do not choose for cloud-native re-architecture. | VMware migration, vSphere, vCenter, SDDC | Outposts, EC2, Application Migration Service |
| EC2 cost optimization | Use right sizing, Savings Plans, Reserved, Spot, Auto Scaling, and scheduling. | Do not use On-Demand for predictable steady workloads if cost matters. | right size, Spot, Savings Plans, stop idle instances | Compute Optimizer, Cost Explorer, Instance Scheduler |
| S3 cost optimization | Use lifecycle policies, Intelligent-Tiering, compression, requester pays, and delete unused data. | Do not store cold data in S3 Standard forever. | lifecycle, archive, storage class, reduce retrieval cost | S3 Storage Lens, Glacier, Intelligent-Tiering |
| EBS cost optimization | Delete unused volumes/snapshots, right-size gp3, use snapshot archive when appropriate. | Do not keep unattached volumes or old snapshots forever. | unattached volume, gp3, snapshot cleanup, archive snapshot | EBS, Data Lifecycle Manager, AWS Backup |
| EFS cost optimization | Use lifecycle management, IA/Archive, One Zone when acceptable, and right throughput mode. | Do not keep all cold files in EFS Standard. | EFS IA, lifecycle, One Zone, throughput mode | EFS Storage Classes, CloudWatch |
| CloudFront caching for cost optimization | Use caching to reduce origin requests, latency, and data transfer costs. | Do not cache highly dynamic/private data without correct cache keys. | reduce origin load, cache hit ratio, lower transfer cost | CloudFront, S3, ALB, API Gateway |
| NAT Gateway cost awareness | Reduce NAT cost by using VPC endpoints for AWS services and per-AZ design. | Do not route large S3/DynamoDB traffic through NAT if gateway endpoints fit. | NAT data processing charge, private subnet egress cost | VPC Endpoints, NAT Instance, Internet Gateway |
| VPC endpoint cost awareness | Use endpoints when they reduce NAT/data transfer cost or improve private security. | Do not assume every endpoint is free; interface endpoints have hourly/data cost. | gateway endpoint free, interface endpoint cost, private access | NAT Gateway, PrivateLink, S3 Endpoint |
| AWS Cost Explorer | Need analyze historical and forecasted AWS costs and usage. | Do not use for automatic budget alerts alone.<br>Use AWS Budgets. | cost analysis, usage trends, forecast, filter by service | AWS Budgets, Cost Anomaly Detection, CUR |
| AWS Cost Anomaly Detection | Need automatically detect unusual AWS spend patterns. | Do not use for fixed monthly budget thresholds only.<br>Use AWS Budgets. | unexpected cost spike, anomaly alert, unusual spend | Cost Explorer, AWS Budgets |
| AWS Budgets concept | Need alerts when cost, usage, RI, or Savings Plans thresholds are reached. | Do not use for deep historical analysis.<br>Use Cost Explorer. | budget alert, monthly cost limit, threshold notification | Cost Explorer, SNS, Cost Anomaly Detection |
| AWS Compute Optimizer concept | Need recommendations to right-size EC2, EBS, Lambda, ECS, and more. | Do not use as direct billing report.<br>Use Cost Explorer. | right-sizing recommendation, overprovisioned, underprovisioned | Trusted Advisor, Cost Explorer, CloudWatch |
| AWS Athena | Need serverless SQL queries over data in S3. | Do not use for high-performance data warehouse with complex optimization.<br>Use Redshift. | query S3, serverless SQL, pay per query, log analysis | Glue Data Catalog, S3, Redshift Spectrum |
| Amazon Redshift | Need petabyte-scale data warehouse and fast analytics SQL. | Do not use for operational transactional workloads. | data warehouse, columnar analytics, BI queries | Athena, QuickSight, Glue, S3 |
| Amazon OpenSearch | Need search, log analytics, and near real-time indexing/querying. | Do not use as relational database replacement. | full-text search, log analytics, Elasticsearch-compatible | CloudWatch Logs, Kinesis Firehose, Athena |
| Amazon EMR | Need managed big data frameworks like Spark, Hive, Presto, or Hadoop. | Do not use when simple SQL over S3 is enough.<br>Use Athena. | Spark, Hadoop, big data cluster, ETL processing | Glue, Athena, Redshift, MSK |
| AWS QuickSight | Need cloud BI dashboards and visual analytics. | Do not use as data warehouse or ETL engine. | BI dashboard, visualization, business intelligence | Redshift, Athena, Glue, S3 |
| AWS Glue | Need serverless ETL, data catalog, crawlers, and data preparation. | Do not use for BI dashboards.<br>Use QuickSight. | ETL, crawler, data catalog, schema discovery | Athena, Lake Formation, S3, Redshift |
| Lake Formation | Need centralized security and governance for data lakes on S3. | Do not use for simple single-bucket permissions only. | data lake governance, fine-grained access, central permissions | Glue Data Catalog, S3, Athena, IAM |
| Amazon Managed Service for Apache Flink | Need real-time stream processing with Apache Flink. | Do not use for simple delivery to S3.<br>Use Firehose. | stream processing, Flink app, real-time analytics | Kinesis Data Streams, MSK, Firehose |
| Amazon Managed Streaming for Apache Kafka | Need managed Apache Kafka-compatible streaming. | Do not choose if app can use simpler Kinesis-native streaming. | Kafka, MSK, existing Kafka apps, streaming platform | Kinesis Data Streams, Amazon MQ, Flink |
| Big data ingestion pipeline | Use Kinesis/MSK for ingestion, Firehose for delivery, S3 for data lake, Glue/Athena/Redshift for analytics. | Do not send high-volume streaming directly to databases if buffering is needed. | ingest, process, store, analyze, streaming pipeline | Kinesis, Firehose, S3, Glue, Athena, Redshift |
| Amazon Rekognition | Need image or video analysis like labels, faces, moderation, or text in images. | Do not use for document form extraction.<br>Use Textract. | image recognition, face detection, content moderation | Textract, Comprehend, SageMaker |
| Amazon Transcribe | Need speech-to-text conversion. | Do not use for text-to-speech.<br>Use Polly. | audio transcription, speech to text, subtitles | Polly, Lex, Comprehend |
| Amazon Polly | Need text-to-speech voice generation. | Do not use for speech-to-text.<br>Use Transcribe. | text to speech, voice output, narration | Transcribe, Lex |
| Amazon Translate | Need automatic language translation. | Do not use for sentiment analysis.<br>Use Comprehend. | translate text, multilingual, language translation | Comprehend, Polly, Transcribe |
| Amazon Lex | Need conversational chatbot with voice or text. | Do not use for general language translation.<br>Use Translate. | chatbot, conversational interface, intent, slot | Polly, Transcribe, Lambda |
| Amazon Comprehend | Need NLP for sentiment, entities, key phrases, language detection. | Do not use for medical text.<br>Use Comprehend Medical. | sentiment analysis, entity detection, NLP | Translate, Kendra, Comprehend Medical |
| Amazon Comprehend Medical | Need extract medical entities and insights from clinical text. | Do not use general Comprehend when medical accuracy/features are required. | medical NLP, clinical text, PHI, medication entities | Comprehend, Textract, HealthLake |
| Amazon SageMaker | Need build, train, tune, deploy, and manage ML models. | Do not use when prebuilt AI service solves the task faster. | custom ML model, training job, endpoint, notebook | Rekognition, Comprehend, Bedrock, Glue |
| Amazon Kendra | Need intelligent enterprise search across documents and knowledge sources. | Do not use for general web CDN or database indexing. | enterprise search, semantic search, document search | OpenSearch, Lex, S3 |
| Amazon Textract | Need extract text, tables, and forms from scanned documents. | Do not use Rekognition for structured document extraction. | OCR, forms, tables, scanned documents | Rekognition, Comprehend, S3 |
| AWS CloudFormation | Need infrastructure as code templates to create and manage AWS resources. | Do not manually create repeatable infrastructure in console. | IaC, stack, template, change set, drift detection | CDK, Terraform, SAM |
| Amazon SES | Need send transactional or marketing email from applications. | Do not use for SMS/push campaign orchestration.<br>Use Pinpoint. | send email, SMTP, transactional email | Pinpoint, SNS, WorkMail |
| Amazon Pinpoint | Need multi-channel customer engagement campaigns like email, SMS, push, and analytics. | Do not use for simple app-to-user email only.<br>Use SES. | marketing campaign, SMS, push notification, customer engagement | SES, SNS, Connect |
| AWS Systems Manager | Need manage EC2/servers at scale: patching, commands, inventory, sessions, parameters. | Do not use SSH manually for fleet-wide operations. | Session Manager, Run Command, Patch Manager, Parameter Store | EC2, IAM, CloudWatch, SSM Agent |
| AWS Outposts | Need AWS infrastructure and services physically on-premises for low latency or data residency. | Do not use when normal AWS Region latency is acceptable. | on-prem AWS, low latency local processing, data residency | Local Zones, Wavelength, VMware Cloud on AWS |
| AWS Batch | Need run batch computing jobs at scale with managed scheduling. | Do not use for always-on web services. | batch jobs, job queue, compute environment, array jobs | Spot Instances, ECS, Fargate, Step Functions |
| AWS AppFlow | Need managed data integration between SaaS apps and AWS services. | Do not use for custom real-time streaming apps.<br>Use EventBridge or Kinesis. | SaaS data transfer, Salesforce to S3, managed flow | EventBridge, Glue, S3 |
| AWS Amplify | Need quickly build and host full-stack web/mobile apps with frontend workflows. | Do not use when you need low-level infrastructure control. | frontend hosting, full-stack app, CI/CD, mobile backend | App Runner, S3 static hosting, CloudFront, Cognito |
| AWS Instance Scheduler | Need automatically start and stop EC2/RDS resources on schedules to save cost. | Do not use for dynamic demand scaling.<br>Use Auto Scaling. | stop at night, start in morning, schedule instances, cost savings | EventBridge Scheduler, Lambda, Auto Scaling |